spring.application.name=catconnect-backend
server.port=8080
server.address=0.0.0.0

# Commented out local profile so Spring Boot connects to Aiven instead of local H2
# spring.profiles.active=local
# spring.config.import=optional:classpath:application-local.properties

# ===================================
# AIVEN MYSQL DATABASE CONFIGURATION
# ===================================
spring.datasource.url=jdbc:mysql://catconnect-db-pawconnect.j.aivencloud.com:25039/defaultdb?sslmode=require 
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_lBZKwBnWOlCB0iYRdJK
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Security & JWT Token Settings
app.jwt.secret=CatConnectSecretKeyChangeThisInProduction2026!
app.jwt.expiration-ms=86400000

# File Storage & CORS Settings
app.storage.path=../storage/images
app.cors.allowed-origins=*

# Multipart File Upload Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB